# BudgetPilot 测试方案

> **版本**: v1.0  
> **日期**: 2026-04-17  
> **目的**: 确保系统核心功能稳定可用，覆盖主要业务场景

---

## 1. 测试策略概览

| 测试类型 | 覆盖范围 | 执行方式 | 频率 |
|----------|----------|----------|------|
| 单元测试 | Service 层核心逻辑 | 自动化 | 每次部署前 |
| 集成测试 | API 接口 + 数据库 | 自动化 | 每次部署前 |
| 功能测试 | 端到端用户流程 | 手动 | 首次部署后 |
| 回归测试 | 已修复的问题 | 手动 | 每次修复后 |

---

## 2. 单元测试修复计划

当前状态：105 个测试中 62 个失败，主要问题是 Mock 配置不完善。

**需要修复的测试文件**：

| 文件 | 问题 | 修复方案 |
|------|------|----------|
| TransactionServiceTest.java | baseMapper 为 null | 添加 @MockBean + @Autowired |
| AccountServiceTest.java | 同上 | 同上 |
| BudgetServiceTest.java | 同上 | 同上 |

---

## 3. 集成测试方案（自动化）

### 3.1 API 接口测试

使用 curl 命令脚本化测试，验证 API 返回格式和数据一致性。

```bash
# 测试脚本示例
#!/bin/bash
API_BASE="http://localhost:6060/api/v1"

# 测试1：创建账户
curl -s -X POST "$API_BASE/accounts" \
  -H "Content-Type: application/json" \
  -d '{"name":"测试账户","type":2,"currency":"CNY"}' | jq '.code'

# 测试2：查询账户列表
curl -s "$API_BASE/accounts" | jq '.data | length'

# 测试3：创建交易
# ...

# 测试4：验证报表缓存清除
# ...
```

### 3.2 数据一致性测试

| 测试场景 | 验证内容 | 预期结果 |
|----------|----------|----------|
| 创建交易 | account.current_balance 变化 | 收入+amount，支出-amount |
| 删除交易 | account.current_balance 回滚 | 反向调整 |
| 余额调整 | 交易记录创建 | category_id=58/59 |
| 余额调整 | 报表缓存清除 | 首页数据实时更新 |

---

## 4. 功能测试方案（手动）

### 4.1 账户管理模块

#### 测试用例 AC-01：新建账户

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击"新增账户" | 弹出新建表单 |
| 2 | 输入名称"测试储蓄卡"，类型"储蓄卡" | 表单正常填写 |
| 3 | 不填写余额字段 | 表单无余额输入框（设计要求） |
| 4 | 点击保存 | 提示"创建成功"，列表显示新账户，余额为 0 |
| 5 | 刷新页面 | 数据持久化，账户仍存在 |

**验证点**：
- API 返回 code=0
- 数据库 `current_balance=0.00`
- 列表显示正确（无乱码）

#### 测试用例 AC-02：编辑账户（无余额变化）

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击已有账户的"编辑" | 弹出编辑表单，显示当前余额 |
| 2 | 修改名称为"测试储蓄卡-v2" | 名称修改成功 |
| 3 | 不修改余额 | 余额保持不变 |
| 4 | 点击保存 | 提示"更新成功"，不弹出余额调整确认框 |

**验证点**：
- 无余额调整交易记录创建
- 报表缓存未清除（当前月份有缓存）

#### 测试用例 AC-03：余额调增

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 编辑账户，余额从 0 改为 1000 | 表单显示新余额 |
| 2 | 点击保存 | 弹出余额调整确认框 |
| 3 | 显示：原余额 0，新余额 1000，变动 +1000 | 计算正确 |
| 4 | 填写原因"初始资金注入" | 原因字段必填 |
| 5 | 点击"确认调整" | 提示"余额已调整" |

**验证点**：
- API 返回 code=0
- 数据库 `current_balance=1000.00`
- 交易记录创建：type=2（收入），category_id=58，amount=1000，note="余额调整: 初始资金注入"
- 首页报表显示"本月收入=1000"

#### 测试用例 AC-04：余额调减

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 编辑账户，余额从 1000 改为 800 | 表单显示新余额 |
| 2 | 点击保存 | 弹出确认框，显示变动 -200 |
| 3 | 填写原因"账单修正" | 原因字段填写 |
| 4 | 确认调整 | 提示成功 |

**验证点**：
- `current_balance=800.00`
- 交易记录：type=1（支出），category_id=59，amount=200
- 首页报表显示"本月支出=200"

#### 测试用例 AC-05：停用账户

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击正常账户的"停用" | 弹出确认对话框 |
| 2 | 确认停用 | 提示"已停用"，按钮变为"启用" |
| 3 | 刷新页面 | 状态显示"停用" |

**验证点**：
- `is_active=0`
- API 返回 code=0（非 204）

#### 测试用例 AC-06：启用账户

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击已停用账户的"启用" | 无确认框，直接启用 |
| 2 | 查看状态 | 显示"正常"，按钮变为"停用" |

**验证点**：
- `is_active=1`
- 无交易记录创建

---

### 4.2 交易管理模块

#### 测试用例 TX-01：新增支出交易

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击"新增交易" | 进入交易表单页 |
| 2 | 选择类型"支出" | 分类下拉显示支出分类 |
| 3 | 输入金额 50，选择账户、分类"餐饮/外卖" | 表单正常 |
| 4 | 点击提交 | 提示成功，跳转到列表页 |

**验证点**：
- 账户余额减少 50
- 首页报表：本月支出增加 50，分类占比显示"餐饮"
- 报表缓存清除

#### 测试用例 TX-02：新增收入交易

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 新增交易，类型"收入" | 分类下拉显示收入分类 |
| 2 | 金额 5000，分类"工资/基本工资" | 提交成功 |

**验证点**：
- 账户余额增加 5000
- 首页报表：本月收入增加，结余变化

#### 测试用例 TX-03：多币种交易

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 新增交易，币种选择"USD" | 币种下拉可用 |
| 2 | 输入金额 100 USD | 提交成功 |
| 3 | 查看列表 | 显示"¥100 USD"和"≈ ¥xxx CNY" |

**验证点**：
- `amount=100, currency=USD`
- `amount_base=100 × 汇率`
- `exchange_rate` 写入正确

#### 测试用例 TX-04：编辑交易

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击已有交易的"编辑" | 表单显示原数据 |
| 2 | 修改金额从 50 改为 80 | 提交成功 |

**验证点**：
- 账户余额变化：+30（差额）
- 报表数据更新

#### 测试用例 TX-05：删除交易

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击交易的"删除" | 弹出确认框"删除将回滚账户余额" |
| 2 | 确认删除 | 提示成功 |

**验证点**：
- 账户余额回滚
- 报表数据更新

---

### 4.3 分类管理模块

#### 测试用例 CT-01：分类树显示

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 进入分类管理页 | 显示分类树结构 |
| 2 | 查看支出分类 | 显示餐饮、交通等大类及其子类 |
| 3 | 点击"全部/支出/收入" tab | 筛选正确 |

**验证点**：
- 树形结构正确（父子关系）
- 中文无乱码
- "余额调整"分类显示（id=58/59）

#### 测试用例 CT-02：编辑分类

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击"餐饮"的"编辑" | 弹出编辑表单 |
| 2 | 修改颜色为红色 | 提交成功 |
| 3 | 查看交易列表 | 分类颜色更新 |

---

### 4.4 报表与首页模块

#### 测试用例 RP-01：首页数据实时性

| 前置条件 | 创建一笔支出交易 100 元 |
|----------|------------------------|

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 创建交易后立即刷新首页 | 本月支出显示 100，无延迟 |
| 2 | 等待 1 分钟再刷新 | 数据一致，缓存未过期 |
| 3 | 删除该交易后刷新 | 本月支出变为 0 |

**验证点**：
- Redis 缓存清除正常
- 报表 API 返回正确

#### 测试用例 RP-02：分类占比饼图

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 创建多笔不同分类的交易 | 首页饼图显示占比 |
| 2 | 检查饼图颜色 | 与分类颜色一致 |

---

### 4.5 预算管理模块

#### 测试用例 BD-01：创建预算

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | POST /api/v1/budgets 创建本月预算 | 返回 code=0，预算 ID |
| 2 | 设置总预算 5000，分类明细 | 数据库记录创建 |

**请求示例**:
```json
{
  "yearMonth": "2026-04",
  "totalAmount": 5000,
  "items": [
    {"categoryId": 1, "amount": 1500},
    {"categoryId": 2, "amount": 500}
  ]
}
```

#### 测试用例 BD-02：查询预算进度

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | GET /api/v1/budgets/2026-04/progress | 返回预算进度数据 |
| 2 | 检查 totalSpent、progressPct | 与交易记录汇总一致 |

**验证点**:
- API 返回 code=0
- totalSpent = 本月已确认支出交易 amountBase 汇总
- items[].spent = 各分类支出汇总

---

### 4.6 周期性交易模块

#### 测试用例 RC-01：创建周期规则

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | POST /api/v1/recurring-rules 创建规则 | 返回 code=0，规则 ID |
| 2 | 设置每月房租 2000，自动确认 | 规则保存成功 |

**请求示例**:
```json
{
  "name": "每月房租",
  "type": 1,
  "amount": 2000,
  "accountId": 2,
  "categoryId": 19,
  "frequency": "MONTHLY",
  "executeDay": 1,
  "startDate": "2026-04-01",
  "autoConfirm": true
}
```

#### 测试用例 RC-02：执行周期规则

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | POST /api/v1/recurring-rules/{id}/execute | 返回 code=0 |
| 2 | 查询交易列表 | 新交易创建，isRecurring=true |
| 3 | 查询账户余额 | 余额减少对应金额 |

**验证点**:
- 交易记录 metadata 包含 `source: "recurring"`
- 账户余额正确更新

---

### 4.7 预警提醒模块

#### 测试用例 AL-01：创建预警规则

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | INSERT INTO t_alert_rule | 规则创建成功 |
| 2 | 设置大额支出阈值 500 | is_active=1 |

**SQL示例**:
```sql
INSERT INTO t_alert_rule (name, type, config, notify_channel, is_active)
VALUES ('大额支出提醒', 2, '{"max_amount": "500"}', 'TELEGRAM', 1);
```

#### 测试用例 AL-02：触发预警

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 创建支出交易金额 > 预警阈值 | 交易创建成功 |
| 2 | 检查 t_alert_log 表 | 预警日志记录插入 |

**验证点**:
- alert_log.is_sent = 1 (Telegram 发送成功)
- 预警标题和内容正确

#### 测试用例 AL-03：预警规则管理页面

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 访问 /alert-rules 页面 | 显示规则列表 |
| 2 | 查看系统预设规则 | 显示7条规则 |
| 3 | 点击"新增规则"按钮 | 弹出创建表单 |
| 4 | 选择规则类型，填写配置 | 表单动态显示配置项 |
| 5 | 点击保存 | 规则创建成功，列表刷新 |

**验证点**:
- API GET /api/v1/alert-rules 返回 code=0
- 规则列表显示规则名称、类型、配置、状态
- 启用/停用按钮正确切换状态

#### 测试用例 AL-04：预警规则API

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | POST /api/v1/alert-rules 创建规则 | 返回 code=0，规则ID |
| 2 | POST /api/v1/alert-rules/{id}/toggle 启用/停用 | 状态切换成功 |
| 3 | DELETE /api/v1/alert-rules/{id} 删除规则 | 删除成功 |

**请求示例**:
```json
{
  "name": "自定义大额预警",
  "type": 2,
  "config": "{\"max_amount\": \"5000\"}",
  "notifyChannel": "TELEGRAM",
  "isActive": true
}
```

---

## 5. 回归测试清单

以下问题是本次发现并修复的，每次部署后必须验证：

| 问题 ID | 描述 | 验证方法 |
|---------|------|----------|
| BUG-001 | 删除账户返回 204 导致前端解析失败 | 删除账户，检查是否提示"已停用" |
| BUG-002 | 编辑账户余额不生效 | 编辑余额，检查数据库 current_balance |
| BUG-003 | 分类树不显示 | 进入分类页，检查树形结构 |
| BUG-004 | 交易列表字段不显示 | 查看交易列表，检查日期/账户/分类列 |
| BUG-005 | 首页报表数据为 0（缓存问题） | 创建交易后刷新首页，检查数据更新 |
| BUG-006 | 余额调整分类乱码 | 查看分类列表，检查"余额调整"中文显示 |
| BUG-007 | 预算 API 返回系统异常 | 查询预算进度，检查返回 code=0 |
| BUG-008 | 预警日志插入失败 | 创建大额支出，检查 t_alert_log 表有记录 |

### BUG-007 修复方案

**问题原因**: Budget.java 实体的 `yearMonth` 字段对应数据库 `year_month` 列，MySQL 中下划线是关键字，需要用反引号包围。

**修复代码** (`src/main/java/uk/gubin/budgetpilot/entity/Budget.java`):
```java
@TableField("`year_month`")
private String yearMonth;
```

### BUG-008 修复方案

**问题原因**: AlertLog.java 继承 BaseEntity，BaseEntity 包含 `createdAt` 和 `updatedAt` 字段，但 `t_alert_log` 表中没有这两个字段。

**修复代码** (`src/main/java/uk/gubin/budgetpilot/entity/AlertLog.java`):
```java
// 移除 extends BaseEntity，直接定义 id 字段
@Data
@TableName("t_alert_log")
public class AlertLog {
    @TableId(type = IdType.AUTO)
    private Long id;
    // 其他字段...
}
```

---

## 6. 测试执行流程

### 6.1 部署前测试（自动化）

```bash
# 1. 运行单元测试（需先修复）
mvn test

# 2. 运行集成测试脚本
./scripts/integration-test.sh

# 3. 检查测试覆盖率
mvn jacoco:report
```

### 6.2 部署后验证（手动）

按顺序执行：
1. 清空测试数据
2. 账户管理测试（AC-01 ~ AC-06）
3. 交易管理测试（TX-01 ~ TX-05）
4. 分类管理测试（CT-01 ~ CT-02）
5. 报表测试（RP-01 ~ RP-02）
6. 回归测试清单验证

---

## 7. 测试数据准备

```sql
-- 初始化测试数据脚本
-- 1. 创建测试账户
INSERT INTO t_account (name, type, currency, current_balance, is_active, sort_order)
VALUES ('测试储蓄卡', 2, 'CNY', 0, 1, 1);

-- 2. 验证分类存在
SELECT id, name, type FROM t_category WHERE id IN (58, 59);

-- 3. 清空测试数据
DELETE FROM t_transaction WHERE note LIKE '%测试%';
DELETE FROM t_account WHERE name LIKE '%测试%';
```

---

## 8. 后续改进计划

| 优先级 | 任务 | 预计工作量 |
|--------|------|------------|
| P0 | 修复单元测试 Mock 配置 | 2h |
| P0 | 添加余额调整 API 单元测试 | 1h |
| P1 | 编写集成测试自动化脚本 | 4h |
| P1 | 添加前端组件测试 | 4h |
| P2 | 添加 E2E 测试（Playwright） | 8h |

---

## 附录：测试报告模板

每次测试后填写：

```
测试日期：YYYY-MM-DD
测试人员：
测试环境：dev / prod

测试结果汇总：
- 账户管理：通过/失败，问题列表
- 交易管理：通过/失败，问题列表
- 分类管理：通过/失败，问题列表
- 报表模块：通过/失败，问题列表
- 回归测试：通过/失败

发现问题：
| ID | 模块 | 描述 | 严重程度 | 状态 |
|----|------|------|----------|------|

建议：
...
```